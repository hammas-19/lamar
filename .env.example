# Supabase is wired up via @nuxtjs/supabase but not used for any real
# queries/auth yet — this is a UI-only build. Fill these in when you're
# ready to connect real data.
SUPABASE_URL=
SUPABASE_KEY=
